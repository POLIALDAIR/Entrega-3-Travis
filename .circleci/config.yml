version: 2.1

jobs:
  build:
    docker:
      - image: cimg/base:stable

    steps:
      - checkout

      - run:
          name: Prueba CircleCI
          command: echo "CircleCI funcionando correctamente, integracion exitosa"

workflows:
  build_test:
    jobs:
      - build